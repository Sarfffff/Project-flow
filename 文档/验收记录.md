# 验收记录

日期：2026-09-11
环境：Windows，Python 3.12.10，Node 24.18.0。

## 自动化检查

Python 及 JavaScript 语法检查通过。

后端集成测试 12 项全部通过：

1. 首次启动为空数据。
2. 项目/任务增删改查、重新初始化后数据保留、完成与重新打开时的进度一致性。
3. 必填项、状态、优先级、日期、字段长度、整数进度、项目引用校验。
4. 可选日期为空时正常创建。
5. 项目编辑、非空项目删除保护。
6. 多窗口 revision 冲突保护。
7. 跨站请求、写入标识与 Content-Type 检查。
8. 批量状态更新与删除。
9. 批量操作中无效 ID 的事务原子性。
10. JSON 备份导出与恢复往返。
11. 错误备份、重复 ID 导致完整回滚。
12. 静态资源可访问，数据库/服务端源码路径不可访问。

前端逻辑检查 20 项全部通过：HTML 转义、逾期判定、真实完成率、负责人搜索、项目名称搜索、组合筛选、未分配项目筛选、优先级排序、空日期排序、项目范围、任务标题注入转义、四列看板、真实图表计数、七日趋势、分页边界、无结果提示、全部页面渲染、主题切换、无效主题回退、已删除项目路由回退。

前端逻辑检查使用 Node VM 和简化 DOM，只验证渲染函数与业务逻辑，不等价于浏览器端到端测试。

## 实际浏览器验收

使用独立 UI 测试数据库，不混入正式工作空间。

- 页面实际加载，显示本地数据库连接成功、空状态、统计卡片和操作入口。
- 中文项目通过真实表单成功保存，并显示成功提示。
- 中文任务及负责人通过真实表单成功保存，并通过 API 回读核对。
- 窄屏视口约 428–434 CSS px 下检查布局；任务编辑窗口截图显示字段和底部保存按钮可见，中文正常。
- 测试发现优先级标签折行，已调整标签列宽；补充屏幕阅读器辅助样式及窄屏收起导航隐藏处理。

## 数据隔离

后端测试在 `tests` 下创建独立临时目录，通过测试清理回调删除测试数据库；服务器从 `src` 加载。正式应用仍使用项目根目录的 `data/flow.sqlite3`，未移动或删除正式数据。先前浏览器验收使用的独立 UI 测试数据库现已清理，以上浏览器结果是首次实现时的验收记录，并非本轮重新执行的浏览器交互测试。

## 整理后复验（2026-09-11）

- 源码移动到 `src`，测试移动到 `tests`，文档与参考资料按用途命名；同步更新启动脚本、数据库定位和测试引用。
- 后端 12 项集成测试重新运行全部通过，前端 20 项逻辑检查及 JavaScript 语法检查重新通过。
- 服务重新启动后，健康接口、数据读取接口、首页、JavaScript、CSS 和图标共 6 个端点均返回 HTTP 200。
- 清理前后及重启后，正式数据库 SHA-256 校验值保持一致。
- 删除旧测试数据、缓存、失败日志、重复源码压缩包和视频处理的中间产物，释放约 117.74 MiB；保留原视频和整理字幕。
- Windows 启动入口增加 `-B`，避免生成 Python 字节码缓存。当前项目目录为唯一维护版本，不再保留重复源码压缩包。

## 验收限制

尚未进行多浏览器兼容矩阵、长时间大规模压测、跨设备协同或生产部署测试。拖拽事件、批量操作与备份恢复的后端/逻辑已覆盖，但并未逐项完成全部真实浏览器手工操作。正式用途为单人本地项目管理，不应直接对公网开放。
