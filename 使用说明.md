# Flow · 项目管理工作空间

根据视频《2分钟VibeCoding项目管理系统》的五步方法实现：功能需求、需求精简、视觉交互、应用生成、功能迭代。

## 运行

需要 Python 3.10 或更新版本。无需安装任何第三方依赖，也无需 Node、npm 或构建步骤。

Windows 下双击项目根目录的“启动项目.cmd”，然后打开 [Flow 项目管理](http://127.0.0.1:58061/project-flow/)。如果应用已经运行，直接打开页面即可，不要重复启动。

也可以在项目根目录运行：

```powershell
python -B src/server.py --port 58061
```

从任意目录运行时，请使用完整路径（以下为示例，请按实际位置调整）：

```powershell
python -B "C:/Projects/Project-flow/src/server.py" --port 58061
```

停止时在运行服务的窗口按 Ctrl+C。再次启动即可恢复使用，数据不会因停止服务丢失。`-B` 用于避免生成 Python 字节码缓存。

这是需要 Python 后端的应用，不是双击 HTML 就能使用的静态演示页面。首次启动为空数据，不会自动生成示例项目或假任务。

## 已实现

- 项目创建、编辑、删除、配色；包含任务的项目禁止直接删除，防止误删。
- 任务创建、编辑、删除，包含描述、项目、负责人、优先级、状态、开始日期、截止日期和进度。
- 四种任务状态：待开始、进行中、待验收、已完成；四档优先级。
- 列表与拖拽看板，列表内直接修改状态；触屏可通过编辑窗口修改状态。
- 搜索任务名称、描述、负责人和项目；按状态、优先级、项目筛选；按创建时间、截止时间、优先级排序；分页及批量更新/删除。
- 实时任务计数、完成率、逾期识别、状态环形图、优先级条形图、近七天完成统计、项目进度、最近动态。
- 深空紫、晨光白、极光绿主题；响应式布局、空状态、错误反馈、表单校验与减少动画支持。
- SQLite 真正持久化，JSON 备份导出与全量恢复；恢复前确认，恢复校验失败则事务回滚。
- 基于全局 revision 的乐观并发控制，避免多个窗口静默覆盖彼此的数据。

## 数据与备份

正式数据库位于项目根目录的 `data/flow.sqlite3`，不是浏览器缓存，也不是 `src/data`。不要删除或随意移动 `data` 目录。刷新浏览器、清理浏览器存储不会删除项目与任务。主题偏好存储在浏览器 localStorage。

推荐在“工作空间设置 → 导出备份”下载 JSON 文件。备份包含项目、任务和任务时间信息，不包含主题与完整活动历史。恢复备份会替换全部现有项目与任务，活动记录重新开始；恢复前请先导出当前数据。

备份恢复限制：文件 7.5 MB 以内，最多 1000 个项目、20000 个任务。恢复不会追加或合并。恢复时会重新校验所有记录；不合法备份不会部分覆盖现有数据。

如果直接复制 SQLite 文件，请先正常停止服务。服务运行时可能存在 WAL 辅助文件，不要只复制主数据库文件；优先使用应用中的 JSON 导出。

## 统计口径

- 完成率：状态为已完成的任务数 / 当前范围任务总数。
- 项目进度：项目内已完成任务数 / 项目总任务数，不是手工进度的平均值。
- 逾期：截止日期早于浏览器本地今天，且任务尚未完成。
- 近期关注：已逾期或未来七天内到期的未完成任务，按截止日期取前三条。
- 完成趋势：按当前已完成任务的完成时间归入本地日期；重新打开或删除任务后统计会改变，并非不可变历史报表。
- 已完成任务进度固定 100%；非已完成任务最大 99%。
- 总览/洞察的统计是全工作空间统计；项目页面是项目统计。任务表格筛选只影响表格或看板，不改变上方统计口径。

## 使用边界

这是单人本地工具，不是团队协同 SaaS。服务仅监听 `127.0.0.1`，没有登录、复杂角色权限、通知推送或跨设备实时同步。另一设备不能直接访问本机回环地址，应通过 With 提供的应用预览访问，或将项目复制到目标电脑运行。

服务使用 JSON 请求、写入请求标识、跨站请求检查、SQL 参数化、静态资源白名单、基本安全响应头。页面对用户文本做转义。不要将此服务直接暴露到公网或企业局域网。共享使用前需要增加认证、授权、TLS、审计和数据库备份策略。

表单在保存期间禁用提交，删除前确认；无自动保存的描述草稿，关闭编辑窗口会丢弃未保存编辑。多窗口不会实时推送刷新，发生版本冲突时会拉取最新数据并提示重新核对后提交。

## 测试

后端测试需要先停止应用，确保 58061 端口空闲。在项目根目录运行：

```powershell
python -B tests/test_server.py
```

测试从 `src` 加载服务器，使用 `tests` 下的独立临时 SQLite 数据库，不接触正式数据；通过测试清理回调删除临时数据库。测试完成后需重新启动应用。如果从其他目录执行，请将脚本路径改为绝对路径。

前端检查需要 Node，仅用于开发测试，不是运行应用的依赖：

```powershell
node --check src/app.js
node tests/test_frontend.cjs
```

前端包含 20 项隔离逻辑检查，覆盖筛选、排序、统计、文本转义、分页、主题与各页面渲染；仅使用内存测试数据，不连接业务数据库，不能替代真实浏览器交互测试。验收范围见“文档”目录中的验收记录。

## 目录结构

```text
project-flow/
  启动项目.cmd          Windows 启动入口
  使用说明.md           运行、备份、测试和目录说明
  src/                  运行源码
    server.py           Python HTTP API 与 SQLite 存储
    index.html          页面框架与编辑对话框
    app.js              页面交互、看板、图表与数据操作
    styles.css          三主题响应式视觉
    favicon.svg         本地品牌图标
    requirements.txt    Python 版本与无第三方依赖说明
  data/                 正式数据，请勿删除
    flow.sqlite3        项目与任务数据库
  tests/                自动化测试
    test_server.py      12 项后端集成测试
    test_frontend.cjs   20 项前端逻辑检查
  文档/                 产品与验收资料
    产品需求.md         功能范围、交互与验收标准
    验收记录.md         实际验证结果与限制
  参考资料/             视频教程原始参考
    教程视频.mp4        原始教程视频
    教程字幕.txt        根据画面 OCR 整理的字幕及校对说明
```

## 文件整理说明

2026-09-11 清理了 Python 缓存、遗留后端/UI 测试数据、旧失败日志、重复源码压缩包，以及抽帧图片、拼图、OCR 中间结果和一次性字幕处理脚本。参考资料保留原视频与整理字幕。

源码集中到 `src`，测试集中到 `tests`，文档与启动入口采用直观中文名称。正式数据库位置保持不变。当前项目文件夹是唯一维护版本，不再保留容易过期的重复源码压缩包。
