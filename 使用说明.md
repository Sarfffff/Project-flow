# Flow · 项目管理工作空间

根据视频《2分钟VibeCoding项目管理系统》的五步方法实现：功能需求、需求精简、视觉交互、应用生成、功能迭代。

## 运行

需要 Python 3.10 或更新版本。无需安装任何第三方依赖，也无需 Node、npm 或构建步骤。

Windows 下双击项目根目录的“打开Flow.cmd”，然后打开 [Flow 项目管理](http://127.0.0.1:58061/project-flow/)。入口会自动复用已运行服务，不会重复启动。项目根目录仅保留这一个打开入口，GitHub 统一在设置中连接。

也可以在项目根目录运行：

```powershell
python -B src/server.py --port 58061
```

从任意目录运行时，请使用完整路径（以下为示例，请按实际位置调整）：

```powershell
python -B "C:/Projects/Project-flow/src/server.py" --port 58061
```

停止时在运行服务的窗口按 Ctrl+C。再次启动即可恢复使用，数据不会因停止服务丢失。`-B` 用于避免生成 Python 字节码缓存。

这是需要 Python 后端的应用，不是双击 HTML 就能使用的静态演示页面。首次启动为空数据，不会自动生成示例项目或假任务。

## 已实现

- 项目创建、编辑、删除、配色；包含任务、需求或缺陷的项目禁止直接删除，防止误删。
- 任务创建、编辑、删除，包含描述、项目、负责人、优先级、状态、开始日期、截止日期和进度。
- 四种任务状态：待开始、进行中、待验收、已完成；四档优先级。
- 项目工作台内提供计划管理、需求管理、缺陷管理三个页签，不新增重复侧栏入口。
- 计划管理复用任务列表与拖拽看板，并增加甘特图视图；列表内直接修改状态，触屏可通过编辑窗口修改状态。
- 甘特图按任务起止日期显示排期，支持日 / 周 / 月尺度、今天定位、时间段切换与点击编辑；未排期任务保留可见。
- 需求包含验收条件，缺陷包含严重程度、发生环境、复现步骤、预期 / 实际结果；均支持增删改查、筛选分页、行内状态和批量操作。
- 新增与详情编辑使用弹窗，项目内切换工作台页签保持项目范围。
- 搜索任务名称、描述、负责人和项目；按状态、优先级、项目筛选；按创建时间、截止时间、优先级排序；分页及批量更新/删除。
- 实时任务计数、完成率、逾期识别、状态环形图、优先级条形图、近七天完成统计、项目进度、最近动态。
- 深空紫、晨光白、极光绿主题；响应式布局、空状态、错误反馈、表单校验与减少动画支持。
- SQLite 真正持久化，JSON 备份导出与全量恢复；恢复前确认，恢复校验失败则事务回滚。
- 基于全局 revision 的乐观并发控制，避免多个窗口静默覆盖彼此的数据。
- GitHub 工作台：个人仓库与源文件浏览、分支提交历史、改动 diff、仓库项目关联、最近提交快照及任务草稿。

## 工作台位置

进入侧栏“项目工作台”，在顶部切换计划管理、需求管理、缺陷管理。计划管理标题右侧切换列表、看板和甘特图；新增按钮固定在模块标题右侧，搜索与筛选位于表格上方。仪表盘仍在原位置，仅增加需求、缺陷摘要卡片。

甘特图仅展示排期并通过弹窗编辑，不自动计算依赖或关键路径；月尺度为 30 天刻度。需求和缺陷状态由用户确认，暂不根据 GitHub 提交自动完成。

## GitHub 接入

在“工作空间设置 → 配置 GitHub 连接”选择公开账号或只读令牌模式。公开模式填写用户名；令牌模式可留空用户名自动识别。选择所需仓库并授予 Contents / Metadata 的 Read-only 权限，在本机设置页密码框输入 Fine-grained token 并点击“验证并连接”。

验证通过立即生效，不需要重启。令牌仅保留服务进程内存，不回显、不保存到浏览器存储、数据库或备份；重启服务后需重新输入。验证失败保留旧连接。断开连接清除本次会话凭据，保留本地仓库关联和快照，不会在 GitHub 撤销授权。

最近提交跟踪保留 100 条快照；可手动同步，也可勾选当前页面可见时每 5 分钟检查当前分支。关闭页面后不后台运行，在线历史支持分页。

## 数据位置与备份

初始数据库位于项目根目录 `data/flow.sqlite3`。在“工作空间设置 → 更改数据库位置”，点击“选择文件夹”或输入新的数据库绝对路径，再确认迁移。选择窗口显示在运行项目的电脑上，并非远程聊天设备。目标文件夹须已存在、目标文件不能存在，文件后缀支持 `.sqlite3`、`.sqlite`、`.db`；仅支持本机磁盘，建议避开云同步目录。

迁移使用 SQLite 在线备份接口，包含 WAL 中已提交的数据。迁移期间暂停其他数据操作，校验成功后立即切换，保留旧数据库和旧安全备份；配置保存失败则继续使用原库。不会覆盖已有数据库，也不直接加载未知数据库。要导入另一个工作空间，请使用 JSON 备份恢复流程。

新位置记录在 `data/storage.json`，重启自动读取。不要删除该配置。若外置盘未接入或文件缺失，启动会提示错误，不会静默创建空库；先接入磁盘或使用 `--db` 显式指定正确路径。

项目管理备份使用 `flow-backup-v2`，包含项目、任务、需求和缺陷；完整工作空间备份使用 `flow-workspace-v3`，还包含 GitHub 账号名称、关联、最近提交快照和活动。均不包含令牌、仓库源码和浏览器主题。恢复先预览确认，再在数据库旁保存完整安全备份，失败则不恢复；项目备份保留快照及仍有效的项目关联。

旧版 `flow-backup-v1` 和 `flow-workspace-v2` 仍可导入，但它们没有需求和缺陷。预览会明确提示继续恢复将清空当前这两类记录，恢复前安全副本会保留它们。新版备份必须使用本版或更新版程序恢复，防止遗漏新增数据。

备份上限 64 MB，最多 1000 项目，任务、需求、缺陷各最多 20000 条，完整备份最多 1000 条仓库分支关联。安全备份不自动删除。运行时请优先使用 JSON 导出，不要只复制可能有 WAL 辅助文件的 SQLite 主文件。

## 统计口径

- 完成率：状态为已完成的任务数 / 当前范围任务总数。
- 项目进度：项目内已完成任务数 / 项目总任务数，不是手工进度的平均值。
- 逾期：截止日期早于浏览器本地今天，且任务尚未完成。
- 近期关注：已逾期或未来七天内到期的未完成任务，按截止日期取前三条。
- 完成趋势：按当前已完成任务的完成时间归入本地日期；重新打开或删除任务后统计会改变，并非不可变历史报表。
- 已完成任务进度固定 100%；非已完成任务最大 99%。
- 仪表盘/洞察的统计是全工作空间统计；项目工作台是相应项目统计。列表、看板和甘特图的筛选不改变上方任务统计口径。
- 需求与缺陷分别统计，不混入计划任务完成率；页签数字为当前范围未完成需求、未关闭缺陷或未完成任务数。

## 使用边界

这是单人本地工具，不是团队协同 SaaS。服务仅监听 `127.0.0.1`，没有登录、复杂角色权限、通知推送或跨设备实时同步。另一设备不能直接访问本机回环地址，应通过 With 提供的应用预览访问，或将项目复制到目标电脑运行。

服务使用 JSON 请求、写入请求标识、跨站请求检查、SQL 参数化、静态资源白名单、基本安全响应头。页面对用户文本做转义。不要将此服务直接暴露到公网或企业局域网。共享使用前需要增加认证、授权、TLS、审计和数据库备份策略。

表单在保存期间禁用提交，删除前确认；无自动保存的描述草稿，关闭编辑窗口会丢弃未保存编辑。多窗口不会实时推送刷新，发生版本冲突时会拉取最新数据并提示重新核对后提交。

## 测试

后端测试需要先停止应用，确保 58061 端口空闲。在项目根目录运行：

```powershell
python -B tests/test_server.py
```

测试从 `src` 加载服务器，使用 `tests` 下的独立临时 SQLite 数据库，不接触正式数据；通过测试清理回调删除临时数据库。测试完成后需重新启动应用。如果从其他目录执行，请将脚本路径改为绝对路径。

前端检查需要 Node，仅用于开发测试，不是运行应用的依赖：

```powershell
node --check src/app.js
node tests/test_frontend.cjs
```

前端包含 20 项隔离逻辑检查，覆盖筛选、排序、统计、文本转义、分页、主题与各页面渲染；仅使用内存测试数据，不连接业务数据库，不能替代真实浏览器交互测试。验收范围见“文档”目录中的验收记录。

无需停止应用的新增专项测试：

```powershell
python -B tests/run_all_offline.py
python -B tests/test_management.py
python -B tests/test_storage_settings.py
node tests/test_management.cjs
node tests/test_github_async.cjs
node tests/test_settings.cjs
```

新增管理测试覆盖数据持久化、旧备份兼容、安全副本、恢复回滚、项目删除保护、甘特日期边界、表单与工作台页签。具体范围见“文档/项目工作台升级.md”。

## 目录结构

```text
project-flow/
  打开Flow.cmd          Windows 启动入口
  使用说明.md           运行、备份、测试和目录说明
  src/                  运行源码
    server.py           Python HTTP API 与 SQLite 存储
    index.html          页面框架与编辑对话框
    app.js              页面交互、看板、图表与数据操作
    management.py       需求与缺陷数据模型、校验和事务操作
    management.js       管理页签、需求缺陷弹窗和甘特图
    management.css      管理模块响应式排版
    styles.css          三主题响应式视觉
    favicon.svg         本地品牌图标
    requirements.txt    Python 版本与无第三方依赖说明
  data/                 正式数据，请勿删除
    flow.sqlite3        项目、任务、需求、缺陷及工作空间数据库
  tests/                自动化测试
    test_server.py      12 项后端集成测试
    test_frontend.cjs   20 项前端逻辑检查
  文档/                 产品与验收资料
    产品需求.md         功能范围、交互与验收标准
    验收记录.md         实际验证结果与限制
  参考资料/             视频教程原始参考
    教程视频.mp4        原始教程视频
    教程字幕.txt        根据画面 OCR 整理的字幕及校对说明
```

## 文件整理说明

2026-09-11 清理了 Python 缓存、遗留后端/UI 测试数据、旧失败日志、重复源码压缩包，以及抽帧图片、拼图、OCR 中间结果和一次性字幕处理脚本。参考资料保留原视频与整理字幕。

源码集中到 `src`，测试集中到 `tests`，文档与启动入口采用直观中文名称。正式数据库位置保持不变。当前项目文件夹是唯一维护版本，不再保留容易过期的重复源码压缩包。
